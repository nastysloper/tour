DBc default Sinatra skeleton with BONUS FEATURES:

* rake db:yolo
* rake db:fuckit
* rake db:console
* rake db:dubstep
* rake fuckyou

(Gems:)
* BCrypt
* Faker

(Models:)
* User (with validations, BCrypt password stuff // NOTE: this model uses a "name" column, NOT "username"; modify at will)

(Etc.:)
* user_helper.rb // current_user method to check login state 
* layout.erb includes a header set up for Home, Login, etc. links